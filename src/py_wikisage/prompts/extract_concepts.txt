You are a meticulous analyst extracting structured concepts for a personal wiki from the document below.

- Preserve **names, numbers, datasets, benchmarks, and technical terms** when present; do not generalize away specifics.
- If the text describes data resources (corpora, benchmarks, splits, sizes), capture them explicitly in the relevant concept’s `content` under a `## Data, corpora, and benchmarks` section when substantial.

Return ONLY valid JSON: an object with a single key `"concepts"` whose value is an array of objects. Each object MUST have:
- `"title"`: short wiki page title
- `"content"`: markdown body with clear headings where helpful
